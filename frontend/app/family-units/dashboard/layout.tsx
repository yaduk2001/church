export default function FamilyDashboardLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <div className="family-dashboard-layout">
            {children}
        </div>
    );
}
